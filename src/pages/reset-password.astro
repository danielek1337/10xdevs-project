---
/**
 * Reset Password Page
 * 
 * Renders the password reset form.
 * Allows users to set a new password after clicking the reset link from email.
 * 
 * Features:
 * - Extracts tokens from URL hash (Supabase sends tokens in URL hash)
 * - Client-side token handling in React component
 */
import Layout from "@/layouts/Layout.astro";
import ResetPasswordView from "@/components/ResetPasswordView";
---

<Layout title="Reset Password - VibeCheck">
  <ResetPasswordView client:load />
</Layout>

